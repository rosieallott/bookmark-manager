<h1> Links </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      <a href="<%= link.url %>"> <%= link.title %> </a>
      Tag: <%= link.tags.map(&:name).join %>
    </li>
  <% end %>
</ul>
