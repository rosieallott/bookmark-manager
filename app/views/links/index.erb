<% if current_user %>
  <h1> Welcome <%= current_user.username %>! </h1>
<% end %>

<h1> Links </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      <a href="<%= link.url %>"> <%= link.title %> </a>
      Tags: <%= link.tags.map(&:name).join(" & ") %>
    </li>
  <% end %>
</ul>
